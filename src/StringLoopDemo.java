//blic class StringLoopDemo {
   //tatic void main(String[] args) {
        //ring VARIABLE = "Solar Radiation ";
        //stem.out.println(VARIABLE);
        //ut.println(VARIABLE.length());
       //or (int i = 0; i < VARIABLE.length(); i++) {
         // char ch = VARIABLE.charAt(i);
        //  System.out.println(ch);
       //
       //nt count =0;
       //or (int v= 0; v<VARIABLE.length();v++){
          //char vo= Character.toLowerCase(VARIABLE.charAt(v));
          //if (vo== "a" || vo=="o" ||vo== "u" || vo =="i" || vo == "e"){
              //count++;
          //}
      //System.out.println(count);
   //}}